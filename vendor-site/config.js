// Адрес сервиса заявок. Сайт — статика на Vercel, а кабинет и виджет живут
// в другом месте, поэтому ссылки «Войти» обязаны быть абсолютными: иначе
// браузер ищет /platform/login на самом сайте и показывает 404.
//
// Здесь был адрес 201.34.133.70.sslip.io — он остался после переезда сервиса
// на собственный домен, и все ссылки «Войти в кабинет» вели на него. Сертификат
// выписан на app.guidecode.ru, поэтому браузер показывал не 404, а красную
// страницу «Невозможно установить безопасное соединение» с кодом
// ERR_CERT_COMMON_NAME_INVALID — худшее, что может увидеть нотариус,
// которому продают работу с паспортами его клиентов.
//
// Урок на будущее: при смене адреса сервиса менять его в трёх местах —
// PUBLIC_BASE_URL на сервере, DEMO_FALLBACK_URL в сайтах нотариусов
// и здесь. Первые два я тогда поменял, третье пропустил.
window.SERVICE_URL = "https://app.guidecode.ru";
