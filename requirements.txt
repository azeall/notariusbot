# --- ядро ---
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
# В Windows нет системной базы часовых поясов, а расписание нотариуса живёт
# в Europe/Moscow — без этого пакета zoneinfo не находит зону.
tzdata==2024.2

# --- веб: админка нотариуса, виджет, приём файлов ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0

# --- каналы ---
aiogram>=3.17,<4
httpx==0.28.1

# --- безопасность: шифрование документов, пароли сотрудников ---
cryptography==44.0.0
argon2-cffi==23.1.0

# --- разработка ---
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
